var year : int := 0
var a : real := 50000000
var b : real := 70000000
const GROWTH := 0.03
const GROWTH2 := 0.02
loop
    a += a * GROWTH
    b += b * GROWTH2
    year += 1
    exit when a >= b
end loop
put "In ", year, " years country A's population will surpass country B."
